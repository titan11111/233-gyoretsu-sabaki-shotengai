# 233-gyoretsu-sabaki-shotengai ｜ 行列さばき商店街

## 一言

自分の店の前にできた行列を、**仕切り（バー）を置いて折りたたむ**パズル。お客をさばききるのが目的だが、
はみ出した列がお隣の店先をふさぐと通報されて営業停止になる。「ごきんじょ迷惑ゲージ」と戦う店番ゲーム。

## 基本情報

| 項目 | 内容 |
|---|---|
| ジャンル | 3D 行列整理パズル（ステージクリア型・全7面） |
| 構成 | `index.html` 1ファイル完結（約1,740行）。WebGL を生で叩く自作3Dエンジン。画像なし |
| サイズ | 公開実体 約3.0MB（20MB鉄則の15%）。うち BGM 2.9MB。**初期ロードは0.07MB**（音楽はタップ時に取得） |
| 画面 | 上段 Canvas（DPR対応）＋ 下段コントロールデッキの2段組。safe-area 追従 |
| 音 | BGM 1曲『商店街の午後』（AAC-LC 128kbps / 3分9秒 / ループ）。効果音なし。ON/OFF は `localStorage` 保存 |
| 保存 | `localStorage['gyoretsu-cleared']`（ステージ別★）、`localStorage['gyoretsu-bgm']`（BGM設定） |
| 様式 | パンク／ランサムノート（切り貼り文字・テープ・NO RULES 系コピー） |

## 勝敗条件

| 判定 | 条件 |
|---|---|
| クリア | 全客の来店が終わり（`spawned >= total` かつ場に客0）、**離脱者が `allowAngry` 以下** |
| 失敗 | ごきんじょ迷惑ゲージが **100%** に到達 → 通報されて営業停止 |
| 失敗 | 左右どちらかのお隣の怒りが **100%** に到達 → 店先をふさぎ続けて通報 |
| 失敗 | 来店終了時に離脱者が `allowAngry` を超過 |

★評価（3段階）
- ★1 = クリア
- ★2 = 離脱 `allowAngry-1` 以下 かつ 迷惑 55%未満
- ★3 = **離脱0人 かつ 迷惑18%未満**

スコア = `さばいた数×120 − 離脱×90 − 迷惑%×6`（大人気モードのみ `+さばいた数×40`）

## コアループ

```
開店前に仕切りを置いて通路を設計（予算＝仕切りの数は有限）
→ 「開店する！」で客が湧き始める
→ 列がはみ出す／お隣の入口をふさぐ → 迷惑ゲージ上昇
→ 待たされた客は離脱（angry）
→ 全員さばききれば勝ち。ゲージ100%で営業停止
```

## 設計の芯：3つの規則

1. **仕切りは有限**
   ステージごとに予算（22〜）が決まっている。壁で囲うのではなく「つづら折り」で密度を稼ぐ設計を強制する

2. **自分の敷地の外で立ち止まると迷惑になる**
   `yard` の外で停止している客の数だけゲージが溜まる（最大 6/秒）。動いている間は加算されず、
   はみ出しが解消されると 2.2/秒 で回復する。＝「一時的なはみ出し」は許されるが「詰まり」は許されない

3. **行きと帰りは別の道が要る**
   入口（`door`）と出口（`exit`）が別座標にあり、帰る客と並ぶ客がぶつかると出口で詰まる。
   面が進むほど敷地が狭くなり、植木（`trees`）とお隣の入口（`keep`＝赤マーカー・封鎖禁止）が柱として効いてくる

## ステージ構成（全7面）

| # | 名前 | 予算 | 客数 | 許容離脱 | 主題 |
|---|---|---|---|---|---|
| 1 | まちかど商店 | 22 | 16 | 2 | 入口を1か所に絞る基礎 |
| 2 | せまい間口 | 26 | 24 | 2 | つづら折りで密度を稼ぐ |
| 3 | 商店街の激戦区 | 32 | 26 | 3 | お隣の入口（赤）を死守 |
| 4 | 雨のち行列 | 34 | 30 | 3 | 植木を柱に折り返す |
| 5 | 角地の行列 | — | — | 3 | 角地レイアウト |
| 6 | 年末大売り出し | — | — | 4 | 客足のピーク処理 |
| 7 | 大人気モード | — | — | 5 | 全面★取得で解禁。開店前から10人待ち |

## 操作

| 操作 | 内容 |
|---|---|
| 🚧 しきりモード | 地面をタップ／ドラッグで仕切りを設置・撤去 |
| ✋ カメラモード | ドラッグで視点回転。⟲⟳＋− で回転・ズーム |
| ×1 / ×2 / ×4 | 開店後の時間倍速 |
| ♪ BGM | 音楽 ON/OFF（設定は保存される） |

## iOS 対応

- `viewport-fit=cover` ＋ `maximum-scale=1,user-scalable=no`（ダブルタップ拡大の抑止）
- `body{touch-action:manipulation; -webkit-touch-callout:none; user-select:none; overscroll-behavior:none}`
- `#gl{touch-action:none}`（キャンバス上のスクロール／ズームを殺す）
- デッキ下端は `env(safe-area-inset-bottom)` 追従
- BGM は**タップ由来でのみ `play()`**（タイトルの「▶ はじめる」／「開店する！」）。`playsinline` 付与
- バックグラウンド復帰時に `visibilitychange` で鳴らし直す

## 既知の未確定事項（公開ブロッカーではない）

- 外部CDN依存: Google Fonts を参照している（オフライン／通信不良時はフォールバック書体になる）
- 効果音（接客・離脱・通報）は未実装。現状 BGM のみ
- ステージ5〜7の数値は本SPECに未転記（コード内 `STAGES` が正）
